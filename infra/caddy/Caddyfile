{
	auto_https off
	admin off
}

:8080 {
	encode zstd gzip

	@zeus_backend path /api/* /auth/* /events/* /health/*
	handle @zeus_backend {
		reverse_proxy {$ZEUS_UPSTREAM:zeus:8081} {
			flush_interval -1
		}
	}

	handle {
		reverse_proxy {$WEB_UPSTREAM:web:3000}
	}
}
